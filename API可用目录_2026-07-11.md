# TopFO 网站 API 可用目录（2026-07-11 实测）

> 测试环境：线上生产 `https://topfo.pages.dev`
> 测试方法：逐端点 curl；登录账号 caiqijun 获取 JWT 后测试受保护接口
> 结论依据：代码静态分析 + 线上实测响应

## 一、基础设施依赖状态（决定哪些接口能用）

| 依赖项 | 状态 | 说明 |
|--------|------|------|
| D1 数据库 `topfo_chat` | ✅ 已迁移且有真实数据 | schools / rankings / programs / chat_messages 均已返回真实数据 |
| 环境变量 `JWT_SECRET` 等 | ✅ 已配置 | login 正常签发 173 字符 JWT |
| Workers AI binding (`env.AI`) | ❌ 未生效 | `env.AI` 调用超时，影响所有真正调用 AI 的接口 |
| Cron Triggers | ⚠️ 未配置 | `wrangler.toml` 无 `[triggers]` 段，定时任务未启用 |

---

## 二、✅ 完全可用（已实测，返回真实数据）

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/api/login` | 公开 | 签发 JWT，已实测拿到 token |
| GET | `/api/rankings` | 公开 | 返回 D1 排名真实数据 |
| GET | `/api/schools` | 公开 | 返回院校 + programs 真实数据 |
| GET | `/api/schools/:id` | 登录 | 单校详情（含 programs、可选 rankings） |
| GET | `/api/programs` | 登录 | 按专业方向/学校分组 |
| GET | `/api/me/data` | 登录 | 返回个人 GPA/雅思/目标专业 |
| GET | `/api/chat` | 登录 | 返回聊天历史（D1 正常） |
| GET | `/api/discussions` | 登录 | 讨论列表（当前为空，接口正常） |
| GET | `/api/reminders` | 登录 | 提醒列表（空，接口正常） |
| GET | `/api/notifications` | 登录 | 通知列表（空，接口正常） |

---

## 三、⚠️ 可用但降级 / 有故障

| 方法 | 路径 | 问题 |
|------|------|------|
| GET | `/api/translations/:lang` | **线上被中间件拦截，必须带 JWT 才能访问**；但前端 `i18n.js` 是匿名调用，导致线上翻译功能实际会失败。需把 `/api/translations` 加入 `_middleware.js` 的 `publicPaths`。 |
| GET | `/api/ai/recommend` | 规则引擎兜底正常（返回 reach/match/safety），但 **AI 部分超时不可用**（`AI 响应超时`），返回的是规则推荐而非真·AI 分析。 |
| POST | `/api/chat` | 读取历史正常；**发消息时 AI 回复依赖 `env.AI`，同 AI binding 不可用，预计返回 502**。 |

---

## 四、🟡 写操作（未实测，逻辑完备，依赖 D1 + 登录，预期可用）

> 为避免污染线上数据，以下写接口未实际 POST。其依赖链（D1 + 已登录）与上面已实测接口一致，逻辑完整。

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| POST | `/api/auth/register` | 公开 | 注册用户 + 建空档案，需 D1 |
| POST | `/api/discussions` | 登录 | 发帖 |
| DELETE | `/api/discussions/:id` | 登录（仅作者） | 软删除 |
| GET/POST | `/api/discussions/:id/comments` | 登录 | 评论列表 / 回复 |
| POST | `/api/reminders` | 登录 | 创建截止提醒 |
| POST | `/api/notifications` | 登录 | 发通知（管理员/系统内部） |
| POST* | `/api/cron/reminders` | cron 触发 | 每日扫描到期申请自动建通知；需 D1 + Cron 触发器（当前未配置） |

---

## 五、根因小结与建议

1. **D1 与鉴权全绿**：所有依赖 D1 的读接口（院校/排名/专业/个人/社区/通知）现在都能正常工作。
2. **AI binding 未生效**：`/api/ai/recommend` 与 `/api/chat`（发消息）的 AI 能力当前不可用，仅规则兜底。需在 Cloudflare 侧确认 Workers AI 绑定已启用且配额充足。
3. **translations 权限 bug**：线上该接口要求登录，但前端匿名调用 → 翻译功能线上失效。修复：在 `functions/api/_middleware.js` 的 `publicPaths` 中加入 `"/api/translations"`。
4. **Cron 未配置**：`/api/cron/reminders` 已就绪，但 `wrangler.toml` 缺 `[triggers]` 段的 cron 调度，定时提醒不会自动跑。
